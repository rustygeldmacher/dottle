#!/bin/bash

if [[ -z "$1" ]]; then
  echo "Usage: dottle <command>"
  echo Please supply a command to run
  echo Run 'dottle help' to see a list
  exit 1
fi

COMMAND_SCRIPT=$(cd "$(dirname "$0")" && pwd)/commands/dottle-$1

$COMMAND_SCRIPT ${*:2}

